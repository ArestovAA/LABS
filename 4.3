#include <stdio.h>
#include <stdlib.h>
#define N 200
int get_simples(int count);


int main ()
{
  int count;
  scanf("%d",&count);
  printf("\n");
  printf("%d\n",get_simples(count));
}

int get_simples(int count)
{
  int ptr[N];
    for(int i = 0; i < N; i++)
    {
      ptr[i] = i;
    }
    ptr[1]= 0;
    for(int s = 2; s < N; s++)
    {
      if(ptr[s] != 0)
      {
        for(int j = s*2; j < N; j += s)          
        {
          ptr[j] = 0;
        }
      }
    }
    for(int i = 0; i < N; i++)
    {
      if(ptr[i] != 0)
      {
        if (ptr[i] < count)
        printf("%d\n", ptr[i]);
      }
    } 
    free (ptr);
  return count;

}
