#include <stdio.h>
#include <stdlib.h> // for exit
long long cache[500]; // cache program

long long get_fibbonachi_via_recursion(int);

int main()
{
  int n;
  printf("Какое число фиббоначи нужно найти: ");
  scanf("%d", &n);
  if (n <= 0)
  {
    printf("error");
    exit(0);
  }
  n++; // n++ because count = 1, if count = 0 program failed 
  for (int count = 1; count < n; count++)
  {
    printf("%llu\n", get_fibbonachi_via_recursion(count));
  }
  return 0;
}

long long get_fibbonachi_via_recursion(int number)
{
  if (cache[number] == 0)
  {
    if (number == 1 || number == 2)
    {
      cache[number] = 1;
    }
    else
    {
      cache[number] = get_fibbonachi_via_recursion(number - 1) + get_fibbonachi_via_recursion(number - 2);
    }
  }

  return cache[number];
}
